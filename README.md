# profile-kelompok-azure

# instalasi
1. buat virtual env dengan perintah python -m venv myenv
2. masuk ke virtual env dengan perintah myenv/Script/activate
3. lalu jalan kan aplikasi dengan perintah python wsgi.py
